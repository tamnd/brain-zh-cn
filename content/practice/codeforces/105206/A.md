---
title: "CF 105206A - \u0417\u0430\u0445\u0430\u0440\u0438\u0441\u043a\u0438\u043d\u044b"
description: "Захар имеет n различных скинов и хочет выбрать ровно k из них。 Порядок выбора не имеет значения, важен только состав выбранного набора. Нужно посчитать，сколько различных наборов из k скинов можно получить。"
date: "2026-06-27T02:39:47+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 105206
codeforces_index: "A"
codeforces_contest_name: "VitebskOpen Junior"
rating: 0
weight: 105206
solve_time_s: 58
verified: true
draft: false
---

[CF 105206A - \u0417\u0430\u0445\u0430\u0440\u0438\u0441\u043a\u0438\u043d\u044b](https://codeforces.com/problemset/problem/105206/A)

 **评级：** -
 **标签：** -
 **求解时间：** 58s
 **已验证：** 是的

 ## 解决方案
 ## 问题理解

 Захаримет`n`различных скинов и хочет выбрать ровно`k`из них。 Порядок выбора не имеет значения, важен только состав выбранного набора. Нужно посчитать, сколько различных наборов из`k`скинов можно получить。 

На вход подаются два целых числа, количество доступных скинов 和 размер выбираемого набора. Нужно вывести число различных способов сделать такой выбор。 

Ограничения очень маленькие,`n`не превышает`20`。 Это означает, что даже полный перебор всех подмножеств из`2^20 = 1 048 576`вариантов вполне возможен。 При этом существует еще более простое математическое решение, поскольку задача сводится к вычислению биномиального коэффициента。 

Есть несколько ситуаций, которые легко реализовать неверно。 

埃斯利`k = 1`， каждый отдельный скин образует свой собственный набор。 

重要的是：```
2 1
```回复:```
2
```Ошибка возникает, если считать перестановки вместо сочетаний.

 埃斯利`k = n`， выбрать можно только все скины сразу。 

重要的是：```
3 3
```回复:```
1
```Некоторые реализации формулы могут ошибиться из-за неверных границ цикла и получить`0`или другое неправильное значение.

 Еще один случай связан с вычислением факториалов。 例如，```
20 10
```说明：```
184756
```我们将在本次活动中为您提供帮助。 Лучше выполнять вычисления только с целыми числами.

 ## 方法

 Самый прямой способ состоит в переборе всех подмножеств множества из`n`элементов。 Для каждого подмножества можно подсчитать количество выбранных элементов 和 увеличить ответ, если их ровно`k`。 Такой алгоритм абсолютно корректен, поскольку рассматривает каждый возможный набор ровно один раз. 普里·马克斯·马利诺姆`n`придется проверить`2^20 = 1 048 576`马可， что также укладывается в ограничения。 

Однако сама задача просит лишь посчитать количество способов выбора. Перебирать сами наборы необязательно, поскольку это классическое число сочетаний。 

Количество способов выбрать`k`элементов из`n`拉夫诺$$C(n,k)=\frac{n!}{k!(n-k)!}.$$塔克卡克`n`诺布尔`20`, факториалы легко помещаются в целые числа Python, асама формула вычисляется практически мгновенно.

 | 方法| 时间复杂度| 空间复杂度| 判决 |
 | --- | --- | --- | --- |
 | 蛮力 | O(2^n) | O(2^n) | O(1) | O(1) | 已接受 |
 | 最佳 | O(n) | O(1) | O(1) | 已接受 |

 ## 算法演练

 1. Считать значения`n`и`k`。 
2. Вычислить`n!`。 Для этого последовательно перемножить числа от`1`Д`n`。 
3. Аналогично вычислить`k!`。 
4. Аналогично вычислить`(n-k)!`。 
5. Подставить полученные значения в формулу биномиального коэффициента 和 выполнить целочисленное деление.
 6. Вывести результат。 

### 为什么它有效

 Биномиальный коэффициент по определению равен числу различных подмножеств размера`k`斯雷季`n`различных элементов。 塔克托里亚尔`n!`считает все возможные перестановки элементов, а деление на`k!`и`(n-k)!`устраняет различия，возникающие только из-за изменения порядка внутри выбранной 和 невыбранной частей。 После этого остается ровно количество различных наборов。 

## Python 解决方案```python
import sys
input = sys.stdin.readline

def fact(x):
    res = 1
    for i in range(2, x + 1):
        res *= i
    return res

n, k = map(int, input().split())

ans = fact(n) // (fact(k) * fact(n - k))
print(ans)
```Функция`fact`вычисляет факториал обычным циклом。 Поскольку максимальное значение равно`20`， выполнение занимает всего несколько десятков операций。 

После чтения входных данных вычисляются три необходимых факториала. Используется целочисленное деление`//`, так как биномиальный коэффициент всегда является целым числом.

 Python 开发工具包 больше，риска переполнения не возникло бы。 В данной задаче максимальный факториал равен`20!`， который также без проблем помещается в стандартные целые типы многих языков。 

## 工作示例

 ### 示例 1

 答案:```
2 1
```| 步骤| n | k | 嗯！ | k！ | （n-k）！ | 回答 |
 | --- | --- | --- | --- | --- | --- | --- |
 | 读取输入| 2 | 1 | - | - | - | - |
 | 计算阶乘 | 2 | 1 | 2 | 1 | 1 | - |
 | 应用公式| 2 | 1 | 2 | 1 | 1 | 2 |

 Здесь существует два возможных набора, каждый содержит один из двух скинов。 

### 示例 2

 答案:```
4 2
```| 步骤| n | k | 嗯！ | k！ | （n-k）！ | 回答 |
 | --- | --- | --- | --- | --- | --- | --- |
 | 读取输入| 4 | 2 | - | - | - | - |
 | 计算阶乘 | 4 | 2 | 24 | 2 | 2 | - |
 | 应用公式| 4 | 2 | 24 | 2 | 2 | 6 |

 Этот пример показывает классический случай。 Из четырех различных скинов можно составить шесть различных пар.

 ## 复杂度分析

 | 测量 | 复杂性 | 说明|
 | --- | --- | --- |
 | 时间 | O(n) | Вычисляются три факториала длиной не более`n`。 |
 | 空间| O(1) | O(1) | Используется только несколько переменных。 |

 Даже при максимальном значении`n = 20`выполняет считанные десятки операций, что значительно меньше любых разумных ограничений времени и 和 памяти。 

## 测试用例```python
# helper: run solution on input string, return output string
import sys
import io

def solve():
    import sys
    input = sys.stdin.readline

    def fact(x):
        res = 1
        for i in range(2, x + 1):
            res *= i
        return res

    n, k = map(int, input().split())
    print(fact(n) // (fact(k) * fact(n - k)))

def run(inp: str) -> str:
    backup_stdin = sys.stdin
    backup_stdout = sys.stdout
    sys.stdin = io.StringIO(inp)
    sys.stdout = io.StringIO()

    solve()

    out = sys.stdout.getvalue()

    sys.stdin = backup_stdin
    sys.stdout = backup_stdout
    return out

# provided sample
assert run("2 1\n") == "2\n", "sample 1"

# custom cases
assert run("1 1\n") == "1\n", "minimum input"
assert run("3 3\n") == "1\n", "choose all elements"
assert run("5 2\n") == "10\n", "standard combination"
assert run("20 10\n") == "184756\n", "maximum boundary"
```| 测试输入| 预期产出 | 它验证了什么 |
 | --- | --- | --- |
 |`1 1`|`1`| 最小允许值 |
 |`3 3`|`1`| 选择每一个元素 |
 |`5 2`|`10`| 通用组合计算|
 |`20 10`|`184756`| 最大输入值|

 ## 边缘情况

 科格达`k = 1`, алгоритм вычисляет```
2 1
```Факториалы равны`2! = 2`,`1! = 1`и`1! = 1`。 科穆拉·沃兹瓦拉埃特`2 / (1 × 1) = 2`。 Это соответствует двум возможным одноэлементным наборам。 

科格达`k = n`, 举例```
3 3
```波鲁查埃姆`3! / (3! × 0!) = 6 / (6 × 1) = 1`。 Факториал нуля корректно равен единице, поскольку цикл в функции сразу завершается и начальное значение`1`остается неизменным。 

Для максимального случая```
20 10
```функция вычисляет три факториала и получает ответ`184756`。 Все вычисления выполняются в целых числах, поэтому ошибки округления отсутствуют, а Python без проблем хранит все промежуточные значения。
