---
title: "CF 102261E - \u0410\u043d\u0430\u043b\u0438\u0437\u0430\u0442\u043e\u0440 \u0438\u0441\u043a\u043b\u044e\u0447\u0435\u043d\u0438\u0439"
description: "Нужно проанализировать небольшую программу на языке EX и для каждой объявленной функции определить, какие типы исключений могут выйти наружу из этой функции."
date: "2026-08-17T20:42:54+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102261
codeforces_index: "E"
codeforces_contest_name: "\u0427\u0435\u043c\u043f\u0438\u043e\u043d\u0430\u0442 \u043f\u043e \u043f\u0440\u043e\u0433\u0440\u0430\u043c\u043c\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u044e - \u043a\u0432\u0430\u043b\u0438\u0444\u0438\u043a\u0430\u0446\u0438\u044f (\u042f\u043d\u0434\u0435\u043a\u0441)"
rating: 0
weight: 102261
solve_time_s: 201
verified: true
draft: false
---

[CF 102261E - \u0410\u043d\u0430\u043b\u0438\u0437\u0430\u0442\u043e\u0440 \u0438\u0441\u043a\u043b\u044e\u0447\u0435\u043d\u0438\u0439](https://codeforces.com/problemset/problem/102261/E)

 **评级：** -
 **标签：** -
 **求解时间：** 3m 21s
 **已验证：** 是的

 ## 解决方案
 ## 问题理解

 Нужно проанализировать небольшую программу на языке EX и для каждой объявленной функции определить, какие типы исключений могут выйти наружу из этой функции. Исключение считается возможным, если существует хотя бы один путь выполнения, на котором оно возникает и не подавляется окружающими`try ... suppress`。 

У программы есть четыре вида конструкций。`maybethrow E`создаёт возможность выбросить исключение`E`。 Вызов функции передаёт наружу любое исключение, которое может выйти из вызываемой функции. Блок`try { ... } suppress E1, E2, ...`удаляет перечисленные исключения из результата своего тела。 Объявления функций находятся только на верхнем уровне, но функции могут вызывать друг друга в любом порядке и могут вызывать самих себя。 

Для каждой функции нужно вывести её имя, количество различных исключений, которые могут выйти из неё, и сами исключения в лексикографическом порядке。 Если различных исключений больше`x`, достаточно вывести только`x`первых。 

奥格拉尼奇尼耶娜`x`и число различных типов исключений равно`1000`, а число вызовов функций также не превосходит`1000`。 Размер исходного текста может достигать миллиона символов。 Это подсказывает разделить задачу на синтаксический разбор, линейный по размеру текста, 和 графовый анализ, зависящий в основном от числа функций, исключений и вызовов. Перебирать все возможные цепочки вызовов или многократно пересчитывать полные множества исключений нельзя, поскольку циклы в графе вызовов могут заставить такой алгоритм делать очень много повторной работы。 

Первый неприятный случай связан с подавлением внутри вызываемой функции. 例如：```
1
2
func f() {
    try {
        maybethrow E
    } suppress E
}
func g() {
    f()
}
```Для`f`ответ пустой, и для`g`тоже пустой。 Исключение существует внутри тела`f`, но оно не выходит из`try`。 Наивный подход, который просто добавляет все`maybethrow`в функцию, ошибочно припишет`E`обеим функциям。 

Второй случай связан с подавлением именно в вызывающей функции:```
1
2
func f() {
    maybethrow E
}
func g() {
    try {
        f()
    } suppress E
}
```У`f`埃斯特`E`, ay`g`его нет。 Нельзя хранить только обычное ребро`g -> f`, потому что одно 和 то же вызываемое исключение может распространяться через один вызов 和 подавляться через другой。 

Третий случай возникает при вложенных обработчиках:```
1
1
func f() {
    try {
        try {
            maybethrow E
        } suppress X
    } suppress E
}
```

`E`подавляется внешним блоком, несмотря на то что внутренний блок подавляет только`X`。 Для конкретного места вызова нужно учитывать все окружающие`try`。 

Наконец, вызовы могут образовывать цикл:```
1
2
func f() {
    g()
}
func g() {
    f()
}
```Здесь ни одна функция ничего сама не выбрасывает, поэтому ответ для обеих функций пуст. Сам факт наличия цикла не создаёт исключение из ничего。 Анализ должен находить только исключения, имеющие реальный источник.

 ## 方法

 Самый прямолинейный вариант состоит в том, чтобы хранить для каждой функции множество известных исключений и многократно просматривать все вызовы。 Если вызываемая функция уже умеет выбрасывать`E`，来自 добавляем`E`вызывающей функции, если конкретный вызов не находится под подавляющим`try`。 

Такой подход корректен, потому что множество возможных исключений монотонно растёт。 В начале известны только исключения из`maybethrow`, после этого информация распространяется по вызовам, а процесс заканчивается, когда больше ничего добавить нельзя。 

Проблема в количестве повторных проходов。 При каждом проходе можно обнаружить только одну новую пару вида“функция может выбросить исключение”。 Таких пар не больше`V * K`, 哥德`V`это число функций, а`K`число типов исключений。 При каждом проходе приходится просматривать до`E`вызовов。 Даже при orgраничениях порядка`1000`это даёт до`10^9`проверок。 

Здесь помогает другая точка зрения。 Зафиксируем один конкретный тип исключения`E`。 Теперь нам не нужно вычислять множества всех исключений одновременно。 Нужно только ответить на вопрос, из каких функций может выйти именно`E`。 

Для этого строим граф вызовов в обратном направлении。 Если функция`f`维亚特`g`, то после нахождения`E`�`g`мы можем попробовать добавить`E`�`f`。 Единственное дополнительное условие состоит в том, что конкретный вызов не должен находиться под обработчиком, 波达夫利亚伊姆`E`。 

Получается обычный обход графа。 Начальные вершины это функции, в которых`E`几天后，我们将在 2020 年 12 月 17 日举行的 2019 年 12 月 12 日举行的新闻发布会上宣布这一消息。 BFS 是由 вызываемых функций к их вызывающим функциям 提供的。 Каждое обратное ребро разрешено только тогда, когда окружающий вызов не подавляет`E`。 

Эта идея также сооответствует опубликованному разбору задачи, где предлагается строить граф вызовов 和 независимо распространять каждое исключение обходом графа。 

После обработки всех типов исключений достаточно проходить их в лексикографическом порядке。 Когда очередная функция получает исключение, добавляем его в ответ только если там ещё меньше`x`элементов。 Поскольку исключения рассматриваются от меньшего к большему, первые`x`добавленных значений автоматически являются требуемыми.

 | 方法| 时间复杂度| 空间复杂度| 判决 |
 | ---| ---| ---| ---|
 | 蛮力 |`O(K * V * E)`|`O(V * K + E)`| 太慢了 |
 | 最佳|`O(S + K * E + P)`|`O(S + V + E + P)`| 已接受 |

 Здесь`S`обозначает размер исходного текста,`K`число типов исключений,`E`число вызовов, а`P`число реально найденных пар“функция может выбросить исключение”。 Кудшем случае`P`orgраничено количеством источников 和 распространений, а при`E <= 1000`графовая часть остаётся небольшой。 

## 算法演练

 1. Сначала разбираем исходный текст и создаём вершину для каждой функции. Поскольку вызов может идти до объявления функциии, при встрече вызова сохраняем имя вызываемой функции, а связываем его с вершиной только после разбора всех объявлений。 
2. Для каждого блока`try`создаём отдельный контекст。 Контекст хранит родительский контекст、список исключений、подавляемых непосредственно этим`try`、 和 исключений， которые возникают непосредственно в его теле。 

Для вызова также запоминаем контекст, в котором этот вызов находится。 Это позволяет отложить обработку`suppress`до завершения синтаксического разбора。 
3. Представляем множество типов исключений внутри контекста одним целым числом. Если типу исключения соответствует бит`i`, то установленный`i`- © бит означает, что этот тип подавляется.

 Это особенно удобно для вложенных`try`。 Эффективная маска контекста равна`effective[parent] | suppress[current]`。 Значит，в ней содержатся все исключения，подавляемые любым обработчиком от текущего места до границы функции。 
4. После завершения разбора вычисляем эффективную маску каждого контекста. Контексты создаются от внешних к внутренним、поэтому родитель уже обработан к моменту вычисления ребёнка。 
5. Для каждого непосредственного`maybethrow`проверяем эффективную маску его контекста。 Если бит исключения отсутствует, это исключение действительно может выйти из функции, в которой находится данный оператор。 Добавляем его в начальные вершины соответствующего исключения。 

Если бит установлен, исключение полностью подавлено ещё до выхода из текущего контекста и не должно становиться источником распространения。 
6. Для каждого вызова после разбора разрешаем имя вызываемой функции и добавляем обратное ребро от вызываемой функции к функции, содержащей вызов。 Вместе с ребром сохраняем эффективную маску контекста вызова。 

Для фиксированного исключения`E`ребро можно использовать тогда и только тогда, когда бит`E`отсутствует в этой маске。 
7. Сортируем все найденные имена исключений лексикографически. Обрабатываем их по одному, начиная с самого маленького.
 8. Для очередного исключения запускаем BFS от всех функций, где это исключение может возникнуть непосредственно。 Каждая посещённая функция может выбросить рассматриваемое исключение。 
9. Из посещённой функции рассматриваем все обратные рёбра. Если конкретный вызов не подавляет рассматриваемое исключение, добавляем функцию, содержащую этот вызов, в очередь。 
10. Когда функция впервые становится посещённой для текущего исключения, добавляем исключение в её ответ, если там ещё меньше`x`элементов。 Повторные посещения для этого же исключения не нужны.
 11. Если у всех функций уже набрано по`x`ответов，оставшиеся типы исключений можно не обрабатывать。 Если хотя бы у одной функции меньше`x`ответов，продолжаем до конца списка исключений。 
12. Для каждой функции печатаем её имя, затем количество сохранённых исключений, затем сами имена в найденном порядке。 Если исключений нет, печатаются имя функции и 和`0`。 

### 为什么它有效

 Рассмотрим фиксированное исключение`E`。 Инвариант BFS состоит в том, что после завершения обхода функция посещена тогда и только тогда, когда существует цепочка реальных источников`E`и вызовов, по которой`E`доходит до этой функции, не попадая на вызов, подавляющий`E`。 

Все начальные вершины являются настоящими источниками`E`, потому что исключение возникает через`maybethrow`и не подавляется его контекстом。 При переходе по обратному ребру мы проверяем именно тот контекст, в котором находится вызов。 Поэтому исключение добавляется к вызывающей функции только в случае, когда оно действительно может пройти через этот вызов。 Обратное направление обхода позволяет пройти любую конечную цепочку вызовов, а циклы безопасно обрабатываются массивом посещённых вершин。 

Значит, после BFS множество посещённых функций точно совпадает с множеством функций, из которых может выйти`E`。 Поскольку все типы исключений обрабатываются в лексикографическом порядке, первые`x`исключений, попавшие в ответ каждой функции, являются`x`лексикографически наименьшими。 

## Python 解决方案```python
import sys
import re
from collections import deque

input = sys.stdin.readline

TOKEN_RE = re.compile(r"[A-Za-z0-9_]+|[{}(),]")

def solve():
    x = int(input())
    n = int(input())

    source = sys.stdin.buffer.read().decode()
    tokens = TOKEN_RE.findall(source)
    m = len(tokens)
    pos = 0

    func_id = {}
    func_names = []

    # Context data.
    # Every function gets one root context.
    parent = []
    owner = []
    suppress = []
    direct_throws = []

    # Each call is (caller_function, callee_name, context_id).
    calls = []

    # Exception name -> bit index.
    exc_id = {}
    exc_names = []

    def get_exc_id(name):
        eid = exc_id.get(name)
        if eid is None:
            eid = len(exc_names)
            exc_id[name] = eid
            exc_names.append(name)
        return eid

    for _ in range(n):
        if tokens[pos] != "func":
            raise ValueError("invalid program")

        name = tokens[pos + 1]
        pos += 2

        # ()
        if tokens[pos] != "(":
            raise ValueError("invalid function header")
        pos += 1
        if tokens[pos] != ")":
            raise ValueError("invalid function header")
        pos += 1

        if tokens[pos] != "{":
            raise ValueError("invalid function body")
        pos += 1

        fid = len(func_names)
        func_id[name] = fid
        func_names.append(name)

        root = len(parent)
        parent.append(-1)
        owner.append(fid)
        suppress.append(0)
        direct_throws.append(0)

        # Stack contains contexts whose blocks are currently open.
        # The root context represents the function body.
        block_stack = [root]

        while block_stack:
            tok = tokens[pos]

            if tok == "}":
                pos += 1
                ctx = block_stack.pop()

                if parent[ctx] != -1:
                    # The closing brace belongs to a try block.
                    if tokens[pos] != "suppress":
                        raise ValueError("missing suppress")

                    pos += 1
                    mask = 0

                    while True:
                        exc_name = tokens[pos]
                        pos += 1
                        mask |= 1 << get_exc_id(exc_name)

                        if tokens[pos] != ",":
                            break
                        pos += 1

                    suppress[ctx] = mask

                continue

            if tok == "try":
                pos += 1
                if tokens[pos] != "{":
                    raise ValueError("invalid try")
                pos += 1

                new_ctx = len(parent)
                parent.append(block_stack[-1])
                owner.append(fid)
                suppress.append(0)
                direct_throws.append(0)

                block_stack.append(new_ctx)
                continue

            if tok == "maybethrow":
                pos += 1
                exc_name = tokens[pos]
                pos += 1

                eid = get_exc_id(exc_name)
                direct_throws[block_stack[-1]] |= 1 << eid
                continue

            # The remaining statement form is a function call:
            # name ( )
            callee_name = tok
            pos += 1

            if tokens[pos] != "(":
                raise ValueError("invalid call")
            pos += 1

            if tokens[pos] != ")":
                raise ValueError("invalid call")
            pos += 1

            calls.append((fid, callee_name, block_stack[-1]))

    # Compute all effective suppression masks.
    # Parents are always created before children.
    effective = [0] * len(parent)

    for ctx in range(len(parent)):
        p = parent[ctx]
        if p == -1:
            effective[ctx] = suppress[ctx]
        else:
            effective[ctx] = effective[p] | suppress[ctx]

    # For every exception, keep the functions where it can originate
    # without already being suppressed.
    seed_by_exc = [[] for _ in exc_names]

    for ctx in range(len(parent)):
        possible = direct_throws[ctx] & ~effective[ctx]
        while possible:
            bit = possible & -possible
            eid = bit.bit_length() - 1
            seed_by_exc[eid].append(owner[ctx])
            possible -= bit

    # Reverse call graph:
    # rev[g] contains calls f -> g as (f, suppression_mask).
    rev = [[] for _ in func_names]

    for caller, callee_name, ctx in calls:
        callee = func_id[callee_name]
        rev[callee].append((caller, effective[ctx]))

    order = sorted(range(len(exc_names)), key=exc_names.__getitem__)

    answers = [[] for _ in func_names]
    seen = [0] * len(func_names)
    stamp = 0

    remaining = len(func_names)

    for eid in order:
        if remaining == 0:
            break

        seeds = seed_by_exc[eid]
        if not seeds:
            continue

        stamp += 1
        bit = 1 << eid

        q = deque()

        for f in seeds:
            if seen[f] != stamp:
                seen[f] = stamp
                q.append(f)

        while q:
            g = q.popleft()

            if len(answers[g]) < x:
                answers[g].append(exc_names[eid])
                if len(answers[g]) == x:
                    remaining -= 1

            for caller, blocked in rev[g]:
                if blocked & bit:
                    continue
                if seen[caller] == stamp:
                    continue

                seen[caller] = stamp
                q.append(caller)

    out = []

    for name, ans in zip(func_names, answers):
        out.append(name + ":")
        out.append(str(len(ans)))
        out.extend(ans)

    sys.stdout.write("\n".join(out))

if __name__ == "__main__":
    solve()
```Парсер использует регулярное выражение только для выделения идентификаторов 和 нескольких знаков пунктуации. Пробелы и переводы строк после этого вообще не имеют значения, поэтому программа одинаково работает с компактным 和 сильно разреженным форматированием исходника。 

Для каждого`try`создаётся контекст с указателем на родителя。 萨姆·斯普伊斯科克`suppress`появляется только после закрывающей`}`, но это не мешает разбору, поскольку операции внутри блока уже ссылаются на созданный контекст. После получения списка подавляемых исключений маска контекста просто заполняется。 

Использование целого числа как множества исключений существенно упрощает работу с вложенными блоками。 说明`effective[parent] | suppress[ctx]`сразу объединяет все окружающие обработчики。 Проверка одного исключения выполняется одной операцией`blocked & bit`。 

Для`maybethrow`сохраняется не отдельная запись на каждое повторение, а битовая маска исключений, которые непосредственно возникают в данном контексте。 Это уменьшает объём памяти, особенно если один 和 тот же тип исключения выбрасывается много раз.

 После разбора имена функций вызовов разрешаются через`func_id`。 Обратный граф нужен потому, что BFS начинается с функций-источников. 埃斯利`g`может выбросить`E`, то нас интересуют функции, которые вызывают`g`, а не функции, которых вызывает`g`。 

马克西夫`seen`не очищается после каждого BFS。 Вместо этого используется возрастающий`stamp`。 Для нового исключения достаточно увеличить`stamp`， после чего старые значения автоматически считаются неактуальными。 Это избавляет от лишнего прохода по всем функциям перед каждым обходом。 

Особенность остановки после`x`ответов состоит в том，что обход для самого исключения всё равно продолжается。 Одна функция может уже иметь`x`результатов，адругая ещё только получить текущий тип。 Исключение нужно распространить полностью, иначе можно случайно пропустить его для другой функции.

 Целочисленное переполнение здесь невозможно: Python 示例 содержит всего до`1000`значащих битов。 

## 工作示例

 ### 示例 1

 Восстановим из приведенного в условии фрагмента полный ввод с`x = 1`и двумя функциями:```
1
2
func func1() {
maybethrow exc1
try {
maybethrow exc2
maybethrow exc3
} suppress exc1, exc2, exc3
maybethrow exc4
}
func main() {
try {
func1()
} suppress exc1
}
```Для`func1`исключения`exc2`и`exc3`появляются внутри блока, который их подавляет。`exc1`появляется вне него, а`exc4`тоже выходит напрямую。 

| 步骤| 当前功能 | 当前异常| 行动| 结果 |
 | ---| ---| ---| ---| ---|
 | 1 |`func1`|`exc1`| 直接投掷，不抑制| 种子`func1`|
 | 2 |`func1`|`exc2`| 直接投掷，抑制| 被忽略 |
 | 3 |`func1`|`exc3`| 直接投掷，抑制| 被忽略 |
 | 4 |`func1`|`exc4`| 直接投掷，不抑制| 种子`func1`|
 | 5 |`main`|`exc1`| 打电话给`func1`,`exc1`压制| 被封锁 |
 | 6 |`main`|`exc4`| 打电话给`func1`,`exc4`不被压抑| 访问`main`|

 普奥斯科利库`x = 1`,`func1`получает только`exc1`, a`main`波鲁查埃特`exc4`。 

Ожидаемый вывод:```
func1:
1
exc1
main:
1
exc4
```Этот пример проверяет сразу два разных уровня подавления。`exc1`Куществует в`func1`, но подавляется уже при вызове из`main`, тогда как`exc4`проходит через тот же вызов。 

### 自定义示例 2

 Рассмотрим вложенный вызов:```
1
3
func a() {
b()
}
func b() {
try {
maybethrow z
} suppress z
maybethrow a
}
func c() {
a()
}
```| 步骤| 功能| 例外| 状态|
 | ---| ---| ---| ---|
 | 1 |`b`|`z`| 压抑在内心`try`, 无种子 |
 | 2 |`b`|`a`| 未受抑制的种子|
 | 3 |`a`|`a`| 反向边缘`a -> b`， 访问`a`|
 | 4 |`c`|`a`| 反向边缘`c -> a`， 访问`c`|

 Итоговые ответы:```
a:
1
a
b:
1
a
c:
1
a
```Здесь BFS показывает、как одно исключение распространяется сразу через несколько уровней вызовов。 

### 自定义示例 3

 Теперь цикл без источника исключения:```
1
2
func f() {
g()
}
func g() {
f()
}
```| 步骤| 功能| 种子 | BFS 结果 |
 | ---| ---| ---| ---|
 |`f`| 无 | 空 | 空 |
 |`g`| 无 | 空 | 空 |

 Ни одна вершина не становится начальной。 BFS 的应用程序包括：

 Ожидаемый вывод:```
f:
0
g:
0
```Этот случай показывает, почему нельзя считать сам факт рекурсивного вызова источником исключения.

 ## 复杂度分析

 | 测量| 复杂性 | 说明|
 | ---| ---| ---|
 | 时间 |`O(S + P + K * E)`|`S`символов разбираются один раз、каждое найденное 和сключение распространяется по обратным рёбрам |
 | 空间|`O(S + V + E + P)`| хранятся контексты、функции、вызовы、обратный граф 和 ответы |

 Здесь`S`это размер програмы,`V`число функций,`E`число вызовов,`K`число типов исключений, а`P`число различных пар "функция может породить непосредственное исключение"。 普里`K <= 1000`и`E <= 1000`графовая часть требует порядка нескольких миллионов проверок。 Разбор исходного текста линейный, поэтому ограничение в миллион символов также укладывается в разумные пределы.

 Python 语言`int`。 Маски содержат не более`1000`比托夫， поэтому объединение масок работает с очень небольшим числом машинных слов。 这是 Python 的一个版本。 

## 测试用例

 Для запуска этих тестов предполагается, что решение сохранено в файле`solution.py`, а функция`solve()`находится в нём на верхнем уровне。```python
import sys
import io
from solution import solve

def run(inp: str) -> str:
    old_stdin = sys.stdin
    old_stdout = sys.stdout

    try:
        sys.stdin = io.StringIO(inp)
        sys.stdout = io.StringIO()
        solve()
        return sys.stdout.getvalue()
    finally:
        sys.stdin = old_stdin
        sys.stdout = old_stdout

# Sample 1
sample = """\
1
2
func func1() {
maybethrow exc1
try {
maybethrow exc2
maybethrow exc3
} suppress exc1, exc2, exc3
maybethrow exc4
}
func main() {
try {
func1()
} suppress exc1
}
"""

assert run(sample) == """\
func1:
1
exc1
main:
1
exc4
""", "sample 1"

# Minimum-size case.
assert run("""\
1
1
func f() {
}
""") == """\
f:
0
""", "minimum empty function"

# Same exception is thrown repeatedly and x keeps only one result.
assert run("""\
1
1
func f() {
maybethrow same
maybethrow same
maybethrow same
}
""") == """\
f:
1
same
""", "duplicate exceptions"

# Suppression at the call site.
assert run("""\
2
2
func f() {
maybethrow A
maybethrow B
}
func g() {
try {
f()
} suppress A
}
""") == """\
f:
2
A
B
g:
1
B
""", "call-site suppression"

# Recursive cycle with a real source.
assert run("""\
3
3
func a() {
b()
}
func b() {
c()
}
func c() {
a()
maybethrow Z
}
""") == """\
a:
1
Z
b:
1
Z
c:
1
Z
""", "recursive cycle"

# Large test: 1000 functions and 1000 distinct exception types.
parts = ["1", "1000"]
for i in range(1000):
    parts.append(
        f"func f{i}() {{\n"
        f"maybethrow e{i}\n"
        f"}}"
    )

large_input = "\n".join(parts) + "\n"

large_expected = []
for i in range(1000):
    large_expected.append(f"f{i}:")
    large_expected.append("1")
    large_expected.append(f"e{i}")

assert run(large_input) == "\n".join(large_expected) + "\n", "large independent case"

# 1000 calls forming one chain, all propagating the same exception.
parts = ["1", "1001"]
for i in range(1000):
    parts.append(
        f"func f{i}() {{\n"
        f"f{i + 1}()\n"
        f"}}"
    )
parts.append(
    "func f1000() {\n"
    "maybethrow E\n"
    "}"
)

chain_input = "\n".join(parts) + "\n"

chain_expected = []
for i in range(1001):
    chain_expected.append(f"f{i}:")
    chain_expected.append("1")
    chain_expected.append("E")

assert run(chain_input) == "\n".join(chain_expected) + "\n", "1000-call chain"
```| 测试输入| 预期产出 | 它验证了什么 |
 | ---| ---| ---|
 | 提供的样品`x = 1`|`func1`得到`exc1`,`main`得到`exc4`| 通过调用进行嵌套抑制和传播 |
 | 一个空函数 |`f:`， 然后`0`| 最小尺寸输入和空结果 |
 | 三个相同`maybethrow same`| 出现 1 次`same`| 异常类型形成一个集合，而不是多重集合 |
 |`g`来电`f`在下面`suppress A`|`f`得到`A,B`,`g`只得到`B`| 附加到特定调用站点的抑制 |
 | 带源的三功能循环`Z`| 三人皆得`Z`| 循环调用图和BFS访问|
 | 1000个独立函数和1000个异常类型 | 每个函数都有自己的异常 | 大量函数和异常类型 |
 | 1000 个呼叫链 | 每个函数都得到`E`| 最大调用次数和长反向传播 |

 ## 边缘情况

 ### 异常在创建的地方被抑制

 Для входа```
1
1
func f() {
try {
maybethrow E
} suppress E
}
```создаётся контекст`try`斯马斯科伊`E`。 У`maybethrow E`эффективная маска также содержит`E`, поэтому выражение`direct_throws & ~effective`удаляет этот бит。 Начальных вершин для`E`BFS 是指 BFS，它的作用是：```
f:
0
```Алгоритм не путает “исключение когда-то было выброшено” с “исключение действительно вышло из функции”。 

### Исключение подавляется только в вызывающей функции

 Для```
1
2
func f() {
maybethrow E
}
func g() {
try {
f()
} suppress E
}
```

`f`波鲁查埃特`E`как непосредственный источник。 Для вызова`g -> f`эффективная маска содержит`E`。 科达 BFS от`f`смотрит обратное ребро, проверка`blocked & bit`оказывается истинной, поэтому`g`не посещается。 

注释：```
f:
1
E
g:
0
```Это показывает, почему фильтр должен храниться вместе с конкретным ребром, а не вместе с вызываемой функцией.

 ### Вложенные обработчики

 Для```
1
1
func f() {
try {
try {
maybethrow E
} suppress X
} suppress E
}
```внутренний контекст получает`X`, а внешний контекст получает`E`。 Эффективная маска внутреннего контекста равна`X | E`。 Бит`E`присутствует в ней, поэтому источник исключения не становится 种子。 

价格：```
f:
0
```Объединение масок по цепочке родителей коректно обрабатывает любое количество вложенных`try`。 

### Рекурсия без источника

 Для```
1
2
func f() {
g()
}
func g() {
f()
}
```ни один контекст не содержит`maybethrow`, поэтому для каждого исключения список 种子 пуст. Цикл не создаёт новые вершины самостоятельно。 

价格：```
f:
0
g:
0
```Это соответствует смыслу "может быть выброшено": должна существовать реальная инструкция, способная породить исключение。 

### Рекурсия с источником

 Для```
1
2
func f() {
g()
}
func g() {
f()
maybethrow E
}
```

`g`становится 种子 для`E`。 BFS идёт по обратному ребру`g -> f`, посещает`f`, затем рассматривает обратное ребро`f -> g`, 诺`g`BFS 是由 BFS 提供的。 

Обе функции получают:```
f:
1
E
g:
1
E
```马克西夫`seen`гарантирует，что цикл не приводит к бесконечному обходу。 

### Больше`x`исключений

 埃斯利```
2
1
func f() {
maybethrow C
maybethrow A
maybethrow B
}
```то все три типа действительно выходят из`f`, но сортировка даёт`A`,`B`,`C`。 普里`x = 2`回复:```
f:
2
A
B
```Обработка исключений в лексикографическом порядке позволяет не выполнять дополнительную сортировку ответа и одновременно гарантирует правильный выбор первых`x`типов。 

### Нет подавления, но несколько разных вызовов

 Если функция вызывает одну функцию под`suppress A`, a другую без обработчика, нельзя хранить единую маску для вызывающей функции. Каждое ребро получает маску своего собственного контекста. Именно поэтому два вызова одной и той же функции могут распространять разные подмножества исключений。 

### Повторное возникновение одного исключения

 埃斯利`maybethrow E`встречается сто раз в одной функции, результат всё равно содержит только одно`E`。 Битовая маска автоматически убирает дубликаты ещё во во время разбора, поэтому размер данных не зависит от количества повторений одного типа исключения。 

### Функция объявлена после вызова

 При разборе вызова имя сохраняется как строка, а разрешение происходит после того, как прочитаны все объявления функций。 共 0页0条记录```
1
2
func a() {
b()
}
func b() {
maybethrow E
}
```обрабатывается так же, как если бы`b`была объявлена первой。 После построения`func_id`伊玛亚`b`успешно превращается в идентификатор вершины графа.
