---
title: "CF 102203H - \u0412\u043b\u0430\u0431\u043e\u0440\u0430\u0442\u043e\u0440\u0438\u0438"
description: "У каждого эксперимента есть одно число (ti)。 Если эксперимент (i) проводится сразу перед экспериментом (j), лаборатория должна перейти от одного оборудования к другому за [ max(ti,tj) ] минут。"
date: "2026-08-18T00:48:10+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102203
codeforces_index: "H"
codeforces_contest_name: "\u0427\u0435\u0442\u0432\u0435\u0440\u0442\u0430\u044f \u041b\u0438\u043f\u0435\u0446\u043a\u0430\u044f \u043a\u043e\u043c\u0430\u043d\u0434\u043d\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u0448\u043a\u043e\u043b\u044c\u043d\u0438\u043a\u043e\u0432 \u043f\u043e \u043f\u0440\u043e\u0433\u0440\u0430\u043c\u043c\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u044e (8-11 \u043a\u043b\u0430\u0441\u0441\u044b)"
rating: 0
weight: 102203
solve_time_s: 92
verified: true
draft: false
---

[CF 102203H - \u0412\u043b\u0430\u0431\u043e\u0440\u0430\u0442\u043e\u0440\u0438\u0438](https://codeforces.com/problemset/problem/102203/H)

 **评级：** -
 **标签：** -
 **求解时间：** 1m 32s
 **已验证：** 是的

 ## 解决方案
 ## 问题理解

 У каждого эксперимента есть одно число (t_i)。 Если эксперимент (i) проводится сразу перед экспериментом (j), лаборатория должна перейти от одного оборудования к другому за

 [
 \max(t_i,t_j)
 ]

 минут。 Подготовка оборудования для первого эксперимента 和 уборка после последнего в суммарное время не входят。 

Значит、если эксперименты идут в порядке (p_1,p_2,\ldots,p_n)、нужно минимизировать

 [
 \sum_{k=1}^{n-1}\max(t_{p_k},t_{p_{k+1}})。 
]

 Нужно вывести не само минимальное время, перестановку номеров экспериментов, которая его достигает。 

При (n\le 3\cdot10^5) перебор перестановок невозможен. Даже алгоритмы (O(n^2)) уже слишком медленные для ограничения в одну секунду, поскольку они потребуют порядка (9\cdot10^{10}) операций в худшем случае. Нам нужен алгоритм порядка (O(n\log n)) или лучше.

 Значения (t_i) могут достигать (10^9)，поэтому решение не должно зависеть от величины самих чисел。 Python обычные целые числа переполнения не вызывают, хотя само значение суммарного времени могло бы быть порядка (3\cdot10^{14}), если бы его требовалось вычислять.

 Первый крайний случай возникает при (n=1)。 Например, для входа```
1
1000000000
```ответом должно быть```
1
```Здесь вообще нет пары соседних экспериментов, поэтому нельзя пытаться обращаться к элементам после первого.

 Другой важный случай связан с одинаковыми значениями。 Для```
4
7 7 7 7
```любая перестановка оптимальна, потому что каждая граница стоит ровно (7)。 Канонический ответ нашего алгоритма будет```
1 2 3 4
```Нельзя требовать единственнную конкретную перестановку, когда несколько вариантов имеют одинаковую стоимость。 

Наконец、нельзя сортировать только сами значения 和 забывать исходные номера。 例如，```
3
5 1 5
```порядок значений (1,5,5) действительно оптимален, но выводить нужно номера экспериментов。 Корректный канонический ответ здесь```
2 1 3
```Если вывести`1 5 5`， это вообще не перестановка номеров экспериментов。 

## 方法

 Самый прямой способ состоит в переборе всех (n!) перестановок。 Для каждой перестановки можно пройти по ее (n-1) соседним парам и вычислить сумму максимумов。 Такой перебор корректен, потому что среди всех рассмотренных перестановок обязательно находитс оптимальная。 Но его трудоемкость равна (O(n!,n)), то есть уже при (n=10) требуется порядка (10!\cdot9=32,659,200) проверок соседних 帕。 При (n=3\cdot10^5) такой подход не имеет практического смысла.

 Можно попробовать строить порядок жадно, но сначала нужно понять, за что именно мы платим. Рассмотрим любую границу между двумя соседними экспериментами。 Ее стоимость равна большему из двух (t), поэтому каждую границу можно мысленно "приписать" тому эксперименту, у которого это большее значение.

 Возьмем все эксперименты и расположим их по возрастанию:

 [
 t_{p_1}\le t_{p_2}\le\ldots\le t_{p_n}。 
]

 В таком порядке стоимость границы между (p_k) 和 (p_{k+1}) равна просто (t_{p_{k+1}})。 Поэтому общая стоимость равна

 [
 t_{p_2}+t_{p_3}+\ldots+t_{p_n}。 
]

 Теперь появляется нижняя граница для любого возможного порядка。 Рассмотрим эксперимент с самым маленьким (t)。 Он может быть единственным экспериментом, который не является большим концом ни одной границы. Каждый другой эксперимент должен хотя бы один раз быть большим концом некоторой соседней пары。 Иначе обе его соседние границы, если они существуют, были бы определены значениями других экспериментов, а сам этот эксперимент оказался бы полностью “неоплаченным”。 

Значит，стоимость любой перестановки не меньше суммы всех (t_i)，кроме одного минимального значения。 То есть

 [
 \text{成本}\ge\sum t_i-\min t_i。 
]

 Именно такую стоимость дает сортировка по возрастанию。 Следовательно, она оптимальна.

 Получается особенно простой результат: нужно только отсортировать эксперименты по (t_i) и вывести их исходные 诺梅拉。 Если значения равны, порядок между ними не имеет значения。 

| 方法| 时间复杂度| 空间复杂度| 判决 |
 | --- | --- | --- | --- |
 | 蛮力 | (O(n!,n)) | (O(n)) | (O(n)) | 太慢了|
 | 最佳| (O(n\log n)) | (O(n)) | (O(n)) | 已接受 |

 ## 算法演练

 1. Сохраняем для каждого эксперимента пару`(t_i, i)`, 哥德`t_i`является временем оборудования, а`i`является его исходным номером。 Номер нужен，потому что в ответе требуется вывести именно перестановку экспериментов。 
2. Сортируем эти пары по`t_i`в неубывающем порядке。 Если несколько значений одинаковы, их взаимный порядок не влияет на стоимость, поэтому любой порядок среди них допустим。 
3. После сортировки берем номера экспериментов в получившемся порядке。 Это и есть требуемая перестановка。 
4. Если (n=1), сортировка содержит один элемент, поэтому алгоритм автоматически выводит единственный номер и не требует отдельной обработки。 

### 为什么它有效

 Пусть эксперименты после сортировки имеют значения

 [
 a_1\le a_2\le\ldots\le a_n。 
]

 В отсортированном порядке стоимость всех границ равна

 [
 a_2+a_3+\ldots+a_n,
 ]

 поскольку на каждой границе справа стоит большее или равное значение.

 Теперь рассмотрим произвольную перестановку。 Для каждой границы ((u,v)) ее стоимость (\max(t_u,t_v)) можно приписать вершине с большим значением。 В линейной цепочке каждый эксперимент, кроме максимум одного, обязан быть большим концом хотя бы одной границы。 Следовательно，в сумму обязательно попадут все значения，кроме одного。 Чтобы сумма была минимальной, неучтенным нужно оставить самое маленькое значение。 Получаем нижнюю границу

 [
 a_2+a_3+\ldots+a_n。 
]

 Отсортированный порядок достигает этой границы, значит он оптимален.

 ## Python 解决方案```python
import sys
input = sys.stdin.readline

def solve():
    n = int(input())
    experiments = list(map(int, input().split()))

    order = sorted(range(n), key=lambda i: experiments[i])

    print(*[i + 1 for i in order])

if __name__ == "__main__":
    solve()
```Сначала считываются все значения`t_i`。 马克西夫`order`содержит индексы от`0`Д`n-1`, отсортированные по соответствующим значениям`experiments[i]`。 Такой вариант удобнее, чем сортировать сами значения, потому что исходные номера сохраняются автоматически.

 沃拉克耶尼耶`i + 1`Python 和 Python 的区别`0..n-1`в требуемую условием нумерацию`1..n`。 

В сортировке нет необходимости явно задавать второй ключ。 普里·拉夫尼赫`t_i`любой порядок оптимален，Python 语言 относительный порядок равных элементов，так что для одинаковых значений получится естественный порядок исходных индексов。 

Алгоритм не вычисляет суммарное время, поскольку задача просит только сам оптимальный порядок. Это дополнительно экономит время исключает ненужные вычисления。 

## 工作示例

 Рассмотрим предоставленный пример:```
5
1 4 5 4 2
```После сортировки экспериментов по времени получаем порядок номеров (1,5,2,4,3)。 

| 步骤| 排序实验 | (t_i) | 当前订单 |
 | --- | --- | --- | --- |
 | 1 | 1 | 1 | 1 |
 | 2 | 5 | 2 | 1, 5 |
 | 3 | 2 | 4 | 1, 5, 2 |
 | 4 | 4 | 4 | 1, 5, 2, 4 |
 | 5 | 3 | 5 | 1、5、2、4、3 |

 普鲁查姆```
1 5 2 4 3
```По формуле из условия стоимость этого порядка равна

 [
 \max(1,2)+\max(2,4)+\max(4,4)+\max(4,5)
 =2+4+4+5=15。 
]

 В опубликованном примере указано значение 14, но оно не соответствует приведенной в условии формуле (\max(t_i,t_j))。 萨姆·沃沃德`1 5 2 4 3`совпадает с сортировкой 和 является оптимальным при формуле из условия。 

最佳答案:```
4
10 1 7 3
```| 步骤| 排序实验 | (t_i) | 当前订单 |
 | --- | --- | --- | --- |
 | 1 | 2 | 1 | 2 |
 | 2 | 4 | 3 | 2, 4 |
 | 3 | 3 | 7 | 2, 4, 3 |
 | 4 | 1 | 10 | 10 2, 4, 3, 1 |

 回复:```
2 4 3 1
```Его стоимость равна

 [
 \max(1,3)+\max(3,7)+\max(7,10)=3+7+10=20。 
]

 Сумма всех значений без минимального равна (3+7+10=20)，поэтому этот порядок достигает доказанной нижней границы。 

## 复杂度分析

 | 测量 | 复杂性 | 说明|
 | --- | --- | --- |
 | 时间 | (O(n\log n)) | Сортировка (n) индексов | Сортировка (n) индексов |
 | 空间| (O(n)) | (O(n)) | Массив индексов и исходные значения |

 При (n\le3\cdot10^5) сортировка выполняет порядка (n\log n) сравнений, что соответствует стандартной эффективной сложности для такого размера входа。 Хранение двух массивов размера (n) также легко укладывается в ограничение памяти 256 МБ.

 ## 测试用例

 Для проверки удобнее сравнивать не только конкретную перестановку, но и ее стоимость, поскольку при одинаковых значениях оптимальных ответов может быть несколько。 При этом для предоставленного примера можно проверить и точный ожидаемый вывод, поскольку наша стабильная сортировка дает именно опубликованный порядок。```python
import sys
import io

def solution(inp: str) -> str:
    data = list(map(int, inp.split()))
    n = data[0]
    t = data[1:1 + n]

    order = sorted(range(n), key=lambda i: t[i])
    return " ".join(str(i + 1) for i in order)

def cost(t, order):
    if len(order) <= 1:
        return 0

    return sum(
        max(t[order[i]], t[order[i + 1]])
        for i in range(len(order) - 1)
    )

# Provided sample
assert solution("5\n1 4 5 4 2\n") == "1 5 2 4 3", "sample"

# Minimum-size input
assert solution("1\n1000000000\n") == "1", "single experiment"

# All values are equal
assert solution("4\n7 7 7 7\n") == "1 2 3 4", "equal values"

# Strictly decreasing input
assert solution("5\n9 8 7 6 5\n") == "5 4 3 2 1", "reverse sorted input"

# Boundary values
assert solution("5\n1000000000 1 999999999 2 1000000000\n") == \
       "2 4 3 1 5", "large values and duplicates"

# Maximum-size input, all values equal
n = 300000
inp = str(n) + "\n" + ("1 " * n).strip() + "\n"
answer = solution(inp)
assert answer == " ".join(map(str, range(1, n + 1))), "maximum n"
```| 测试输入| 预期产出 | 它验证了什么 |
 | --- | --- | --- |
 |`1 / 1000000000`|`1`| Минимальный размер 和 отсутствие соседних пар |
 |`4 / 7 7 7 7`|`1 2 3 4`| Одинаковые значения и несколько оптимальных ответов |
 |`5 / 9 8 7 6 5`|`5 4 3 2 1`| Необходимость сортировки по значениям, ане по исходным позициям | Необходимость сортировки по значениям, ане по исходным позициям |
 |`5 / 1000000000 1 999999999 2 1000000000`|`2 4 3 1 5`| Большие значения、повторяющийся максимум 和 корректная индексация |
 |`300000 / 1 1 ... 1`|`1 2 ... 300000`| Верхняя граница (n) 和 линейное хранение ответа |

 ## 边缘情况

 Для (n=1) вход```
1
1000000000
```содержит только один эксперимент。 После сортировки список индексов остается`[0]`, поэтому после перехода к нумерации с единицы получается`1`。 Здесь нет ни одной границы, стоимость равна нулю, и алгоритм не пытается обращаться к несуществующему соседу.

 Для одинаковых значений рассмотрим```
4
7 7 7 7
```Сортировка сохраняет индексы`1,2,3,4`。 Каждая из трех границ имеет стоимость (7), поэтому общая стоимость равна (21)。 Любая другая перестановка дала бы ту же суму。 Это показывает, почему при равных`t_i`не нужно придумывать специальное правило。 

Для уже отсортированного по убыванию массива```
5
9 8 7 6 5
```алгоритм меняет его порядок на```
5 4 3 2 1
```Значения идут как (5,6,7,8,9), астоимость равна (6+7+8+9=30)。 Любой другой порядок не может быть дешевле, поскольку сумма всех значений без минимального равна той же нижней границе。 

Для больших значений и повторов рассмотрим```
5
1000000000 1 999999999 2 1000000000
```Сортировка дает значения

 [
 1,2,999999999,1000000000,1000000000
 ]

 和诺梅拉```
2 4 3 1 5
```Две последние позиции имеют одинаковое значение (10^9), поэтому их можно было поменять местами без изменения ответа。 В выбранном порядке каждая граница оплачивается своим большим концом, и достигается нижня граница

 [
 2+999999999+1000000000+1000000000。 
]

 Главная ошибка, которую этот случай выявляет, состоит в сортировке самих`t_i`без сохранения номеров。 Ответ должен содержать`2 4 3 1 5`， не последовательность времен。 

Для максимального размера (n=300000) сортировка остается единственной существенной операцией. При одинаковых значениях все индексы сохраняют ес​​тественный порядок, а память используется линейно。 Это как раз тот случай，ради которого нельзя применять (O(n^2)) построение или перебор перестановок。
