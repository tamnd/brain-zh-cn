---
title: "CF 102920A - 自动驾驶汽车"
description: "算法 H 通过维护一个弱递减序列（其条目为正且总和始终为 $n$）来生成 $n$ 的所有整数分区 $a1 ge cdots ge am ge 1$。"
date: "2026-07-04T07:54:50+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102920
codeforces_index: "A"
codeforces_contest_name: "2020-2021 ACM-ICPC, Asia Seoul Regional Contest"
rating: 0
weight: 102920
solve_time_s: 68
verified: false
draft: false
---

[CF 102920A - 自动驾驶车辆](https://codeforces.com/problemset/problem/102920/A)

 **评级：** -
 **标签：** -
 **求解时间：** 1m 8s
 **已验证：** 否

 ## 解决方案
 ## 解决方案

 算法 H 生成所有整数分区$a_1 \ge \cdots \ge a_m \ge 1$的$n$通过维护一个弱递减序列，其条目为正且其总和始终为$n$。 获取分区$n$最多进入$m$部分，正确的模型是允许尾随零并用$k \le m$作为序列的零件$a_1 \ge \cdots \ge a_k \ge 1$按零扩展长度$m$。 

因此状态空间必须从严格的正部分变为弱递减的非负部分$a_1 \ge \cdots \ge a_m \ge 0$与总和$n$。 的每个分区$n$最多进入$m$零件唯一对应于这样一个零件$m$-通过用零填充元组，以及每个这样的$m$-tuple 通过删除尾随零来对应于分区。 

因此，算法 H 中所需的修改仅限于初始化，因为所有后续变换都保留顺序和总和，并且从不依赖于严格的正性$a_j$除非使用积极性来描述初始配置。 

修改后的初始化取代了以下要求：除第一个部分之外的所有部分至少$1$从一开始就允许空零件。 分区的正确启动配置$n$最多进入$m$parts 是所有弱递减元素中按字典顺序排列的最大元素$m$- 元组求和为$n$，即单个部分等于$n$后面跟着零。 

因此修改后的步骤H1为：$$a_1 \leftarrow n,\quad a_j \leftarrow 0 \text{ for } 2 \le j \le m,\quad a_{m+1} \leftarrow -1.$$所有其他步骤 H2 到 H6 均保持不变。 

正确性源于转换规则的不变性。 H2 到 H6 中的每个操作都保留条件$a_1 \ge \cdots \ge a_m$和$\sum_{j=1}^m a_j = n$，因为每次更新都包括将一个单元从一个位置转移到较早的位置或重新分配正减量$x = a_j - 1$跨越早期指数，同时保持非递增顺序，如第 7.2.1.4 节中的原始分析所示。 这些步骤都不需要约束$a_j \ge 1$，只有这样的比较$a_j \ge a_1 - 1$并减少$a_q \leftarrow a_q - 1$保持有意义，一旦成立$a_j$被允许到达$0$。 

的每个分区$n$最多进入$m$零件产生独特的弱递减$m$-非负整数元组，并且修改后的算法为每个可达配置准确地生成一个这样的元组，因为 H 中的逆字典级数系统地枚举了前导部分的所有可行减少，同时在后面的组件之间重新分配质量而不违反单调性。 由于初始状态是此顺序中唯一的最大值，并且每一步都会产生下一个可接受的配置，因此遍历涵盖所有且仅有效的配置$m$-元组。 

因此，修改后的初始化将算法 H 从分区扩展为精确的$m$最多可划分为正部分$m$积极的部分，完成所需的建设。 这样就完成了证明。 ∎
